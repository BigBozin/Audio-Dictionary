from setuptools import setup, find_packages

setup(
    name="audio_dictionary",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies from requirements.txt here
    ],
)